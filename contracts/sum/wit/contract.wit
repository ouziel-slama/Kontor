package kontor:contract;

world contract {
    include kontor:built-in/built-in;

    record sum-args {
        x: u64,
        y: u64,
    }

    record sum-return {
        value: u64,
    }

    export sum: func(args: sum-args) -> sum-return;
}
