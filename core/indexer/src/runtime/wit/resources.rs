pub struct ViewStorage {
    pub contract_id: i64,
}

pub struct ProcStorage {
    pub contract_id: i64,
}

pub struct ViewContext {
    pub contract_id: i64,
}

pub struct ProcContext {
    pub contract_id: i64,
    pub signer: String,
}
